{
  "name": "@rehearsal-ai/worker",
  "version": "1.0.0",
  "description": "Simple Node worker that polls DB for pending jobs and calls an AI provider",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "dev:worker": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "start:worker": "node dist/index.js",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "axios": "^1.6.0",
    "dotenv": "^16.3.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@rehearsal-ai/server": "*",
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "zod": "^3.22.0"
  }
}
